# Backend Final Verdict Table

This table provides a comprehensive, authoritative verdict for every material finding, claim, and proposal across the entire audit and implementation history of the AAC Assistant backend.

---

## Master Verdict Table

| ID | Topic | Previous Claim | Current Evidence | Independent Verdict | Action |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **VRD-01** | **File Upload Deletion Leak** | V1 claimed `remove_owned_upload` silently aborts deletion when `uploads_dir = config.UPLOADS_DIR`. | All 5 production callers in `symbols.py` pass `config.UPLOADS_DIR / "symbols"`, where `uploads_dir.name` matches `parts[0]`. Docstring clarified in `file_uploads.py`. No files leak in production. | **FALSIFIED_IN_PRODUCTION** | `NO_ACTION_REQUIRED` (Preserve current docstring and code) |
| **VRD-02** | **AI Board Generation Item Count** | V1 claimed strict item count check (`len(valid_items) != item_count`) is brittle and proposed relaxing to a lower bound. | AAC grid dimensions require exact item capacity. Relaxing lower bound violates `test_generate_board_items_rejects_incomplete_item_count` and `AGENTS.md` mandate against silent fallback data. | **REFINED_CONTRACT_INVARIANT** | `NO_ACTION_REQUIRED` (Retain strict equality check) |
| **VRD-03** | **Startup Schema Migration Scans** | V1 claimed deduplication scans in `schema.py` create boot bottlenecks and proposed adding a schema version tracking table. | `schema.ensure()` executes in 173ms on boot. A migration version table adds unnecessary upgrade/downgrade state risks for portable SQLite installations. | **REJECTED_AS_OVERENGINEERING** | `NO_ACTION_REQUIRED` (Retain lightweight self-healing checks) |
| **VRD-04** | **`UserService` & `users.py` Deletion** | V1 claimed `users.py` and `UserService` are 100% dead / redundant copies of `auth_users.py` and proposed deletion. | Actively consumed by frontend React components: `Achievements.tsx:97` (`/users/students`), `Students.tsx:222` (`POST /users/students`), `Students.tsx:253` (`POST /users/reset-password`). Provides distinct teacher roster RBAC. | **FALSIFIED_LIVE_DEPENDENCY** | `NO_ACTION_REQUIRED` (Retain `users.py` and `UserService`) |
| **VRD-05** | **Board Creation Route Placement** | V1 proposed moving `POST /api/boards` from `board_ai.py` to `boards.py`. | `POST /boards` orchestrates AI board generation; moving it couples `boards.py` to AI provider subsystems. Both routers mount under `/api/boards` seamlessly. | **REJECTED_COUPLING_RISK** | `NO_ACTION_REQUIRED` (Retain in `board_ai.py`) |
| **VRD-06** | **LM Studio Compatibility Route** | V1/V2 deleted `GET /api/providers/ai/models/lmstudio` as an unconsumed duplicate of `/settings/ai/models/lmstudio`. | Restored in Stage A. While internal frontend calls `/settings/ai/models/lmstudio`, preserving the `/api/providers/` route maintains 100% backward compatibility for external tooling and legacy clients. | **PRESERVED_COMPATIBILITY_ROUTE** | `NO_ACTION_REQUIRED` (Keep compatibility route) |
| **VRD-07** | **Analytics Log Compatibility Route** | V1/V2 deleted `POST /api/analytics/log` as a dead duplicate of `/usage`. | Restored in Stage A. Modern frontend calls `/usage`, but retaining `/log` as an alias delegating to `_log_usage_request` maintains backward compatibility with zero overhead. | **PRESERVED_COMPATIBILITY_ROUTE** | `NO_ACTION_REQUIRED` (Keep compatibility route) |
| **VRD-08** | **`nullcontext(db)` Wrapper Artifact** | V1/V2 identified `with nullcontext(db) as session:` in `achievements.py` as an incomplete refactoring artifact. | Replaced 5 instances with `session = db` and removed unused `nullcontext` import. Simplified code with zero transaction change. | **VERIFIED_CLEANUP** | `NO_ACTION_REQUIRED` (Keep implemented simplification) |
| **VRD-09** | **Dead Method `_get_hardcoded_default`** | V1/V2 identified `_get_hardcoded_default()` in `template_manager.py` as dead unreferenced code. | 0 references across entire codebase; `TemplateManager.__init__` validates `default.yaml` on startup. Method was deleted cleanly. | **VERIFIED_DEAD_CODE_DELETION** | `NO_ACTION_REQUIRED` (Keep deletion) |
| **VRD-10** | **Provider `close()` Lifecycle Unification** | Subclass implementations had inconsistent signatures (`close` sync in Ollama vs async in OpenRouter). | Added `async def close(self) -> None: await self.close_async()` on `BaseLLMProvider` and cleaned up divergent subclass overrides. Supports polymorphic async cleanup. | **VERIFIED_LIFECYCLE_IMPROVEMENT** | `NO_ACTION_REQUIRED` (Keep unified base method) |
| **VRD-11** | **Smartbar Closure Allocation** | V1 proposed refactoring helper closures in `analytics.py:L175-265` to reduce heap allocation. | Closure creation takes $<1\mu\text{s}$, negligible compared to 50ms database queries. Refactoring rejected as micro-optimization. | **REJECTED_MICRO_OPTIMIZATION** | `NO_ACTION_REQUIRED` (Keep readable handler closures) |
| **VRD-12** | **Board Generation Type Annotation** | V1 noted `BoardGenerationService.__init__` omitted `GroqProvider` from union type. | Broadened parameter typing to `llm_provider: BaseLLMProvider`. Verified accurate for all 4 providers. | **VERIFIED_TYPING_POLISH** | `NO_ACTION_REQUIRED` (Keep broadened annotation) |
| **VRD-13** | **Teacher Learning RBAC Scope** | V2 found teachers blocked on progress/history; Turn 2 over-broadened to `/start` and mutation; Stage A scoped to read-only. | Verified in `access.py:get_learning_session_or_404(allow_teacher=True)` and `learning.py`. Teachers have read-only access to assigned students (`/progress`, `/history`), while session creation (`/start`) and mutations (`/ask`, `/answer`, `/end`) remain strictly restricted to student self / admin. 8/8 regression tests pass. | **VERIFIED_LEAST_PRIVILEGE_RBAC** | `NO_ACTION_REQUIRED` (Keep least-privilege scoping) |
| **VRD-14** | **V3 Production Files Denominator** | V3 claimed 104 production files. | 103 Python files in `src/` (including `src/scripts/account_admin.py` and `src/config.py`) + 1 file `launcher.pyw` = exactly 104 files. (16 files in `scripts/` are maintenance/operator scripts outside `src/`). | **VERIFIED_DENOMINATOR** | `NO_ACTION_REQUIRED` |
| **VRD-15** | **V3 Production Symbols Denominator** | V3 claimed 777 symbols. | Independent AST analysis confirms 136 classes + 374 sync module functions + 36 async module functions + 209 sync methods + 22 async methods = 777 top-level and class-method definitions. (Omitted 33 nested functions, 1 property, 74 script definitions). | **VERIFIED_WITH_QUALIFICATION** | `NO_ACTION_REQUIRED` |
| **VRD-16** | **V3 FastAPI Operations Denominator** | V3 claimed 126 operations across 67 unique application paths. | Live app registers **127 operations** across **103 distinct registered paths** (100 normalized paths). V3 omitted `WEBSOCKET /api/collab/boards/{board_id}` and copied a stale path count from V1. | **CONTRADICTED_DENOMINATOR** | `UPDATE_AUDIT_RECORDS` (Acknowledge WebSocket route and true path count) |
| **VRD-17** | **V3 Auth Matrix Quality** | V3 claimed 126/126 routes mapped with 100% verified least-privilege enforcement. | V3 Auth Matrix omitted WebSocket route and contained shallow mechanical classifications (marked `Student Self: YES` on achievement admin routes based on top-level dependency, missing handler-level 403 checks for students). Real code enforces checks properly. | **PARTIALLY_SUPPORTED_AUDIT_ARTIFACT** | `NO_CODE_ACTION_REQUIRED` (Code is secure; audit notes documented) |
| **VRD-18** | **V3 Mutation Inventory Denominator** | V3 claimed 231 mutation sites across 14 logical mutation flows. | Independent regex search confirms exactly 231 SQLAlchemy mutation calls (63 add, 67 commit, 11 delete, 28 execute, 4 file_delete, 41 flush, 6 remove_owned_upload, 11 rollback). Direct file writes (9 sites) are managed in dedicated helpers. | **VERIFIED_DENOMINATOR** | `NO_ACTION_REQUIRED` |
| **VRD-19** | **Destructive Operations Safety** | V3 claimed 52 sites across 6 logical destructive flows are 100% safe. | Traced 16 distinct destructive/state-clearing API operations and 1 startup repair flow. All enforce proper authorization, preconditions, and atomic rollback/post-commit cleanup. | **VERIFIED_TRANSACTION_SAFETY** | `NO_ACTION_REQUIRED` |
| **VRD-20** | **V3 Test File Mapping Accuracy** | V3 File Inventory listed corresponding tests for all 104 files. | V3 mechanically synthesized test file names matching `tests/test_<basename>.py`, which do not exist as files (e.g. `tests/test_common.py`, `tests/test_history.py`, `tests/test_account_admin.py`). Real tests exist in differently named files (e.g. `test_learning_common_helpers.py`). | **FALSE_AUDIT_ARTIFACT_MAPPING** | `DOCUMENT_EVIDENCE_QUALITY` (Do not rely on V3 test column) |
| **VRD-21** | **Authentication & Session Revocation** | Security versioning guarantees instant session revocation on credential changes. | Verified in `jwt_utils.py`, `credential_service.py`, `auth_service.py`, and `access.py`. Password change / reset increments `security_version`, revoking existing JWTs across all devices. Live DB user lookups enforce instant role demotions. | **VERIFIED_SECURITY_INVARIANT** | `NO_ACTION_REQUIRED` |
| **VRD-22** | **SQLite Concurrency & Lock Management** | WAL mode and PRAGMA settings prevent lock contention in desktop usage. | `PRAGMA journal_mode=WAL`, `busy_timeout=30000`, `foreign_keys=ON`, `check_same_thread=False` configured in `db.py`. `NullPool` used in test fixtures to avoid connection pooling leaks. | **VERIFIED_PERSISTENCE_INVARIANT** | `NO_ACTION_REQUIRED` |
| **VRD-23** | **Packaged Windows Runtime Paths** | `resolve_runtime_root()` cleanly separates read-only bundle (`_MEIPASS`) from writable runtime root. | Verified in `src/config.py:L20-74`. Correctly identifies frozen binary, redirects `%APPDATA%` under `Program Files`, and preserves portable USB folder execution. | **VERIFIED_PACKAGING_INVARIANT** | `NO_ACTION_REQUIRED` |
| **VRD-24** | **Export / Import HMAC Integrity** | Export/import payloads are protected by HMAC-SHA256 and float normalization. | Verified in `src/api/routers/export_import.py`. Normalizing floats (`0.0` $\to$ `0`) ensures stable checksums across browser JSON parse/stringify roundtrips. 10MB request bounding and pre-validation prevent corrupt imports. | **VERIFIED_DATA_PORTABILITY** | `NO_ACTION_REQUIRED` |
| **VRD-25** | **Production LLM Provider Enforcement** | Groq is the strict production LLM provider invariant per `AGENTS.md`. | Verified in `src/config.py`, `src/api/deps/providers.py`, `src/aac_app/providers/groq_provider.py`. Warmup fails with `degraded` if model is unconfigured; constructor allows key-only initialization for `/settings/ai/models/groq`. | **VERIFIED_PRODUCTION_INVARIANT** | `NO_ACTION_REQUIRED` |

---
